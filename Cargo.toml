[package]
name = "rust-compressor"
version = "0.1.0"
edition = "2024"

[dependencies]
zstd = "0.13"
rayon = "1.10"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "compression_bench"
harness = false
